/**
 * 听页「从 / 到」按列表序号区间朗读（展开后选起止条数）。
 * false 时隐藏该面板；「▶ 列表朗读」仍为一键从第 1 条播放到末尾。
 */
export const enableListSpeakSequenceRange = false

/** 播放器底部「复制 / 清空播放日志」；false 时隐藏。 */
export const enablePlayerDebugLogButtons = false
